{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://tuvren.dev/schema/diagnostic-trace/1.0.0/schema.json",
  "title": "Tuvren Diagnostic Trace",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "traceId",
    "createdAt",
    "terminalProfile",
    "redaction",
    "replay",
    "capture",
    "rootCorrelation",
    "records",
    "snapshots",
    "wrapCount"
  ],
  "properties": {
    "schemaVersion": { "const": "1.0.0" },
    "traceId": { "type": "string", "minLength": 1 },
    "createdAt": { "type": "string", "format": "date-time" },
    "sdkVersion": { "type": "string" },
    "terminalProfile": {
      "$ref": "https://tuvren.dev/schema/terminal-profile/1.0.0/schema.json"
    },
    "redaction": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "fullContent",
        "input",
        "clipboard",
        "terminalPayloads",
        "environment",
        "absolutePaths"
      ],
      "properties": {
        "fullContent": { "type": "boolean" },
        "input": { "enum": ["redacted", "included"] },
        "clipboard": { "enum": ["redacted", "included"] },
        "terminalPayloads": { "enum": ["redacted", "included"] },
        "environment": { "const": "redacted" },
        "absolutePaths": { "const": "redacted" }
      }
    },
    "replay": {
      "type": "object",
      "additionalProperties": false,
      "required": ["runtime", "applicationInput"],
      "properties": {
        "runtime": { "enum": ["available", "redacted"] },
        "applicationInput": { "enum": ["available", "redacted"] }
      }
    },
    "capture": {
      "type": "object",
      "additionalProperties": false,
      "required": ["startedAt", "completePrefix"],
      "properties": {
        "startedAt": { "enum": ["context-initialization", "attached"] },
        "completePrefix": { "type": "boolean" }
      }
    },
    "rootCorrelation": {
      "type": "object",
      "additionalProperties": false,
      "required": ["contextId", "initialSequence", "initialRecordId"],
      "properties": {
        "contextId": { "type": "string", "minLength": 1 },
        "initialSequence": { "type": "string", "pattern": "^[0-9]+$" },
        "initialRecordId": { "type": "string", "minLength": 1 }
      }
    },
    "wrapCount": { "type": "integer", "minimum": 0 },
    "records": {
      "type": "array",
      "maxItems": 262144,
      "items": { "$ref": "#/$defs/record" }
    },
    "snapshots": {
      "type": "array",
      "maxItems": 256,
      "items": {
        "$ref": "https://tuvren.dev/schema/diagnostic-snapshot/1.0.0/schema.json"
      }
    }
  },
  "$defs": {
    "record": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "sequence",
        "timestampNanos",
        "kind",
        "correlation",
        "payload"
      ],
      "properties": {
        "sequence": { "type": "string", "pattern": "^[0-9]+$" },
        "timestampNanos": { "type": "string", "pattern": "^[0-9]+$" },
        "kind": {
          "enum": [
            "context",
            "input",
            "event",
            "command",
            "effect-span",
            "reconcile",
            "transaction",
            "mutation",
            "dirty",
            "layout",
            "text",
            "render",
            "diff",
            "terminal-write",
            "error",
            "cleanup",
            "unattributed"
          ]
        },
        "correlation": {
          "type": "object",
          "additionalProperties": false,
          "required": ["recordId", "contextId"],
          "properties": {
            "recordId": { "type": "string", "minLength": 1 },
            "contextId": { "type": "string", "minLength": 1 },
            "parentRecordId": { "type": "string", "minLength": 1 },
            "eventId": { "type": "string", "minLength": 1 },
            "commandId": { "type": "string", "minLength": 1 },
            "commandInstanceId": { "type": "string", "minLength": 1 },
            "effectSpanId": { "type": "string", "minLength": 1 },
            "transactionId": { "type": "string", "minLength": 1 },
            "renderRequestId": { "type": "string", "minLength": 1 },
            "componentId": { "type": "string", "minLength": 1 },
            "subjectId": { "type": "string", "minLength": 1 },
            "subjectKind": { "enum": ["component", "text-document"] }
          }
        },
        "payload": {
          "type": "object",
          "maxProperties": 8,
          "$comment": "validateDiagnosticTraceRecords enforces the exact kind-selected payload contract"
        }
      }
    }
  }
}
